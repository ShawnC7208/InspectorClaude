---
description: Open the InspectorClaude local coaching dashboard.
argument-hint: [source]
allowed-tools: ["mcp__plugin_inspectorclaude_inspectorclaude__show_dashboard"]
---

# Open InspectorClaude Dashboard

Open InspectorClaude as a visual coaching dashboard.

Use the `show_dashboard` tool with `source: "all"` unless the user names a specific source.

Explain that InspectorClaude is local-first and that Chat/Cowork history appears only when provided through explicit input, supported exports, checkpoints, or supported local session files.
